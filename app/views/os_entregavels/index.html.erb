<p id="notice"><%= notice %></p>

<h1>Listando Os Entregavels</h1>

<table class="table table-bordered table-hover table-condensed">

  <thead>
    <tr>
      <th>Entregavel</th>
      <th>Osi d</th>
      <th>Ust previsto</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @os_entregavels.each do |os_entregavel| %>
      <tr>
        <td><%= os_entregavel.entregavel_id %></td>
        <td><%= os_entregavel.osi_d %></td>
        <td><%= os_entregavel.ust_previsto %></td>
        <td><%= link_to 'Show', os_entregavel %></td>
        <td><%= link_to 'Edit', edit_os_entregavel_path(os_entregavel) %></td>
        <td><%= link_to 'Destroy', os_entregavel, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Os entregavel', new_os_entregavel_path %>
