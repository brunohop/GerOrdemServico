<p id="notice"><%= notice %></p>

<h1>Listando Os Deb Tecnicos</h1>

<table class="table table-bordered table-hover table-condensed">

  <thead>
    <tr>
      <th>Debt</th>
      <th>Os</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @os_deb_tecnicos.each do |os_deb_tecnico| %>
      <tr>
        <td><%= os_deb_tecnico.debt_id %></td>
        <td><%= os_deb_tecnico.os_id %></td>
        <td><%= link_to 'Show', os_deb_tecnico %></td>
        <td><%= link_to 'Edit', edit_os_deb_tecnico_path(os_deb_tecnico) %></td>
        <td><%= link_to 'Destroy', os_deb_tecnico, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Os deb tecnico', new_os_deb_tecnico_path %>
