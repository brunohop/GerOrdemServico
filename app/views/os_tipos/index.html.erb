<p id="notice"><%= notice %></p>

<h1>Listando Os Tipos</h1>

<table class="table table-bordered table-hover table-condensed">

  <thead>
    <tr>
      <th>Os tipo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @os_tipos.each do |os_tipo| %>
      <tr>
        <td><%= os_tipo.os_tipo %></td>
        <td><%= link_to 'Show', os_tipo %></td>
        <td><%= link_to 'Edit', edit_os_tipo_path(os_tipo) %></td>
        <td><%= link_to 'Destroy', os_tipo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Os tipo', new_os_tipo_path %>
