<p id="notice"><%= notice %></p>

<h1>Listando Catalogos</h1>

<table class="table table-bordered table-hover table-condensed">

  <thead>
    <tr>
      <th>Idem catalogo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @catalogos.each do |catalogo| %>
      <tr>
        <td><%= catalogo.nome %></td>
        <td><%= link_to 'Exibir', catalogo %></td>
        <td><%= link_to 'Editar', edit_catalogo_path(catalogo) %></td>
        <td><%= link_to 'Excluir', catalogo, method: :delete, data: { confirm: 'Você tem certeza?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Catalogo', new_catalogo_path %>
